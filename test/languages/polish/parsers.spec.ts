/* 

		it("the Polish Wiktionary.", async () => {
			const results = await Wiktionary.get("być", { siteLanguage: "pl", lemmaLanguage: "język polski" });

			expect(results).to.not.be.undefined;
		});*/
