import * as cheerio from "cheerio";
import { Entry, EntrySectionSkeleton, Lemma } from "../../../types.js";

export default function parse(_: cheerio.CheerioAPI, __: EntrySectionSkeleton, ___: Lemma): Entry[] | undefined {
	return undefined;
}
